#include <stdio.h>
int main()
{
   int x,y,sum,subtract,multiply,divide;
   scanf("%d%d",&x,&y);
  sum=(x+y);
  printf("sum=%d\n",sum);
  subtract=(x-y);
  printf("subtract=%d\n",subtract);
  multiply=(x*y);
  printf("multiply=%d\n",multiply);
  divide=x/y;
   printf("divide=%d\n",divide);
    return 0;
}
